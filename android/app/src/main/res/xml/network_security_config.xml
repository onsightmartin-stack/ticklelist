<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Block all cleartext (HTTP) traffic by default — enforce HTTPS everywhere. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Explicitly allow HTTPS to the production domain. -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">ticklelist.org</domain>
        <domain includeSubdomains="true">onsightmartin.com</domain>
        <domain includeSubdomains="true">lovable.app</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
